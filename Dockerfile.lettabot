FROM node:20-alpine

RUN apk add --no-cache git curl && \
    npm install -g npm@latest

WORKDIR /app

RUN git clone https://github.com/letta-ai/lettabot.git . && \
    npm ci && \
    npm run build && \
    npm link

CMD ["lettabot", "server"]
