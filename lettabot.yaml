# LettaBot Configuration
server:
  mode: docker
  baseUrl: http://letta-server:8283

agent:
  name: LettaBot
  model: openai/gpt-4o-mini

channels:
  telegram:
    enabled: true
    token: ${TELEGRAM_BOT_TOKEN}
    dmPolicy: open

features:
  cron: false
  heartbeat:
    enabled: false
